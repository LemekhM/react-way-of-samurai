const FOLLOW = 'FOLLOW';const UNFOLLOW = 'UNFOLLOW';const SET_USERS = 'SETSTATE';const SET_NUMBER_PAGE = 'SET-NUMBER-PAGE';const SET_TOTAL_USERS_COUNT = 'SET-TOTAL-USER-COUNT';export const followActionCreator = (userId) => ({type: FOLLOW, userId});export const unFollowActionCreator = (userId) => ({type: UNFOLLOW, userId});export const setUsersActionCreator = (users) => ({type: SET_USERS, users});export const setNumberPageActionCreator = (page) => ({type: SET_NUMBER_PAGE, page});export const setTotalUsersCountActionCreator = (users) => ({type: SET_TOTAL_USERS_COUNT, users});let initialState = {    users: [],    totalUsersCount: 0,    pageSize: 100,    numberPage: 1};const usersReducer = (state = initialState, action) => {    switch (action.type) {        case FOLLOW: {          return {              ...state,              users: state.users.map(u => {                   if (u.id === action.userId) {                      return {...u, followed: true}                  }                   return u              })          }        }        case UNFOLLOW: {            return {                ...state,                users: state.users.map(u => {                    if (u.id === action.userId) {                        return {...u, followed: false}                    }                    return u                })            }        }        case SET_USERS: {            return {                ...state, users: action.users            }        }        case SET_NUMBER_PAGE: {            return {                ...state, numberPage: action.page            }        }        case SET_TOTAL_USERS_COUNT: {            return {                ...state, totalUsersCount: action.users            }        }        default:            return state    }};export default usersReducer;